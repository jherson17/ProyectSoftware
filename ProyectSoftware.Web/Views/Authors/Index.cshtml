@model IEnumerable<ProyectSoftware.Web.Data.Entities.Author>

@{
    ViewData["Title"] = "Index"; // Establece el título de la página.
    Layout = "~/Views/Shared/_MyLayout.cshtml"; /* forma para que en ves del tener el layout normal tenga el otro */
}
@* //estan heredando de layaout *@
<div class="pagetitle">
    <h1>Dashboard</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-asp-controller="Home" asp-action="Dashboard">Home</a></li>
            <li class="breadcrumb-item active">Dashboard</li>
        </ol>
    </nav>
</div><!-- End Page Title -->

<section class="section dashboard">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Authors</h5>
                    <!-- Table Variants -->
                    <table class="table table-bordered">
                        <thead class="table-primary">
                            <tr>
                                <th>@Html.DisplayNameFor(model => model.StageName)</th>
                                <th>@Html.DisplayNameFor(model => model.Name)</th>
                                <th>@Html.DisplayNameFor(model => model.LastName)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="table-secondary">
                                    <td class="table-success">@Html.DisplayFor(modelItem => @item.Name)</td>
                                    <td class="table-danger">@Html.DisplayFor(modelItem => @item.LastName)</td>
                                    <td class="table-warning">@Html.DisplayFor(modelItem => @item.StageName)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <!-- End Table Variants -->
                </div>
            </div>
            <!-- End Table Variants -->
</section>

    